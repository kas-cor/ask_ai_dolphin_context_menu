# Default AI model for opencode.
# List available models: opencode models
# Examples:
#   opencode/deepseek-v4-flash-free   (free, default)
#   opencode/deepseek-v4-flash
#   opencode/claude-sonnet-4-6
#   opencode/claude-haiku-4-5
#   opencode/gpt-5.4-pro
#   opencode/gemini-3.5-flash
#   opencode/qwen3.5-plus
export ASK_MODEL="opencode/deepseek-v4-flash-free"

# UI language for the PyQt5 dialog and Konsole runner.
# Uncomment to force Russian (auto-detected from $LANG by default):
# export ASK_LOCALE="ru_RU"
# Or to force English:
# export ASK_LOCALE="en_EN"

# UI theme for the PyQt5 dialog (dark/light).
# By default, auto-detected from your system theme.
# Uncomment to override:
# export ASK_THEME="dark"
# export ASK_THEME="light"

# ask / askr use ask-dolphin-run.sh (from the Ask AI Dolphin Context Menu project).
# This script adds the current directory (PWD) as file context for the AI.
# Requires prior installation: ./install.sh (see README.md)
#
# Enable by adding to your shell config:
#   echo 'source ~/.ask_ai' >> ~/.bashrc
ask() {
  ~/.local/bin/ask-dolphin-run.sh "$*"
}

# Raw mode (no glow formatting)
askr() {
  GLOW_DISABLED=1 ~/.local/bin/ask-dolphin-run.sh "$*"
}
